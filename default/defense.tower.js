module.exports ={
  run: function(tower, hostiles){

      if(hostiles.length > 0){
       tower.attack(hostiles[0]);
      }
      //var cpuUsed2 = Game.cpu.getUsed();console.log("CPU Used before " + tower+": " + cpuUsed2);






  }
}
